#pragma once
#include <stdlib.h>

#ifndef MAXPATHLEN
#define MAXPATHLEN 260
#endif

#ifndef MIN
#define MIN(a, b) (((a) < (b)) ? (a) : (b))
#endif

#ifndef MAX
#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#endif